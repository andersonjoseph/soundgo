CreateSessionDto:
  type: object
  properties:
    username:
      description: username
      type: string
      minLength: 3
      maxLength: 32
    password:
      description: user password
      type: string
      format: password

SessionToken:
  type: object
  properties:
    token:
      description: auth token
      type: string
