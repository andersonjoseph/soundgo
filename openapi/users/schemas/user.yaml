type: object

required:
  - id
  - username
  - email
  - createdAt

properties:
  id:
    type: string
  username:
    type: string
    minLength: 3
    maxLength: 16
  email:
    type: string
    format: email
  createdAt:
    type: string
    format: date-time
